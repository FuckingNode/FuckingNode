import { rgb8 } from "@std/fmt/colors";

export const orange = (str: string): string => rgb8(str, 202);
export const brightOrange = (str: string): string => rgb8(str, 215);
export const pink = (str: string): string => rgb8(str, 213);
